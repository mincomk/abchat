[package]
name = "dbridge-persistence"
version = "0.1.0"
edition = "2024"

[dependencies]
dbridge-core = { path = "../dbridge-core" }
dbridge-error = { path = "../dbridge-error" }
async-trait = "0.1.86"
serde = { version = "1.0", features = ["derive"] }
